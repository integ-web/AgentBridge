{
  "name": "@agentbridge/sdk",
  "version": "0.1.0",
  "description": "AgentBridge TypeScript SDK — connect agents to browser workspaces",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agentbridge/core": "*"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  },
  "license": "MIT"
}
