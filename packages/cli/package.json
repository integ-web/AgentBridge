{
  "name": "@agentbridge/cli",
  "version": "0.1.0",
  "description": "AgentBridge CLI — command-line tool for agent browser management",
  "main": "dist/index.js",
  "bin": {
    "agentbridge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@agentbridge/core": "*",
    "commander": "^12.0.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  },
  "license": "MIT"
}
